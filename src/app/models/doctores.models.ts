export class Doctor {
    id!: string;           // Un identificador único para el doctor
    nombre!: string;       // Nombre completo del doctor
    especialidad!: string; // Especialidad del doctor
    telefono!: string;     // Número de teléfono del doctor
}
